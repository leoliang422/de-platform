# Roadmap v2 · 从"学习平台"到"学习社区"

> 本文件在 M0–M4 上线（Vercel + Render + Neon）之后，规划后续迭代。已与需求方确认基线决策，作为 M5+ 的开发依据。
> 关联：[roadmap.md](roadmap.md)（M0–M4）、[spec.md](spec.md)、[architecture.md](architecture.md)、[points-and-payment.md](points-and-payment.md)。

## 已确认的基线决策

| 项 | 决策 |
|---|---|
| 支付通道 | 接入**微信支付 + 支付宝**（需求方已有商户资质）；开发期用官方沙箱/测试环境 |
| 运行实例 | 后端升级 **Render Starter（约 $7/月）常驻**，支撑真异步 Worker 与私聊 WebSocket 长连接 |
| 异步队列 | **Upstash Redis + 独立 Worker** 服务，`TASK_QUEUE_ENABLED=true` |
| 方向 | 认可"学习平台 → 学习社区"整体方向；**私聊/社区优先级提前** |
| 大模型 | 暂保持 mock，后续切真实豆包（改环境变量即可） |

优先级标注：`P0` 上线必备 · `P1` 体验关键 · `P2` 增强。

---

## 里程碑（私聊已提前）

- **M5 · 支付 + 异步队列真实化**
- **M6 · 社区地基（账号安全 + 富媒体 + 通知）** —— 私聊的前置依赖
- **M7 · 私聊 IM + 社交关系（提前）**
- **M8 · 互动运营 + 内容完善 + 社区扩展**
- **M9 · 合规 / 观测 / 运营后台（持续）**

---

## M5 · 支付 + 异步队列

### 5A 真实支付（微信 + 支付宝）
现状：`MockProvider` 永远成功；订单 `Order` / 权益 `Entitlement` / 双标价 / 积分扣减闭环已就绪。

**本轮（占位式）已完成：**
- [x] 抽象层升级：`ChargeResult`（同步结算 / 异步 pay_url·qr）+ `CallbackResult` + `get_payment_provider()` 按 env 分派并**安全回退 mock**
- [x] 微信（v3 Native）/ 支付宝（page.pay）Provider 骨架 + 占位配置（`create_charge`/`parse_callback` 标 `TODO(M5-real)`）
- [x] 现金解锁分同步（mock 立即发权益）/ 异步（挂起订单返回 `pay_url`）两条路径
- [x] **异步回调 webhook** `POST /payment/webhook/{provider}`：幂等结算订单 + 发放权益
- [x] 前端 `UnlockPanel` 适配 `pending` 态（引导跳转支付页）
- [x] `docs/deployment.md` 补微信/支付宝凭证获取方式 + 回调地址；`.env.example` 占位变量
- [x] 测试：现有支付用例全绿 + 工厂回退 + webhook 结算/幂等/失败/未知通道

**待真实凭证到位后补齐（TODO(M5-real)）：**
- `P0` 补微信 v3 Native 真实下单 + APIv3 验签解密；支付宝 page.pay 签名 + 公钥验签
- `P0` 前端收银台二维码展示 + 支付结果轮询/回跳页
- `P1` 退款、对账流水、订单超时关闭；订单状态机扩展（refunded/closed）
- 完成标准：沙箱支付成功 → 回调 → 订单 `paid` → `entitlement` 自动发放；重复回调不重复发放。

### 5B 异步队列
现状：ARQ + Redis Worker 代码已在，默认同步。

**本轮已完成：**
- [x] 投稿加工逻辑抽成 `SubmissionService.process()`（同步路径与 Worker **共用同一套**，天然幂等：仅处理 `processing` 态）
- [x] `_dispatch()`：`TASK_QUEUE_ENABLED=true` 时入队，**入队失败自动回退同步加工**（不阻断投稿）
- [x] `_enqueue()` 正确关闭连接池（`aclose`）；Worker 任务改为委托 `service.process`
- [x] 加工失败落 **`failed`** 态并记录原因；新增 `POST /submissions/{id}/retry`（作者/管理员）
- [x] 前端「我的投稿」：processing 轮询刷新、failed 展示原因 + 重试按钮
- [x] `render.yaml` 备好 worker 服务块（注释式）+ `REDIS_URL`；`deployment.md` 补 Upstash + Worker 开启流程
- [x] 测试：入队分支/入队失败回退/幂等/失败落态/retry 全覆盖

**待真实 Upstash + Worker 上线时执行（按 deployment.md）：**
- `P0` 建 Upstash Redis、web+worker 填 `REDIS_URL`、取消注释 worker、置 `TASK_QUEUE_ENABLED=true`（建议升 Starter）
- `P1` 死信/重试上限；更多任务类型（邮件、支付回调后处理、通知扇出）
- 完成标准：投稿提交立即返回 `processing`，Worker 异步完成后 `pending_review`，可查询/重试。

---

## M6 · 社区地基（账号安全 + 富媒体 + 通知）

私聊/社区强依赖以下能力，先行补齐。

### 账号与安全
- [x] `P0` 用户/管理员**修改密码**（`PATCH /users/me/password`，校验原密码/新旧不同）
- [x] `P0` 个人资料编辑（昵称、简介、职业标签、头像 URL；`PATCH /users/me`）+ 公开主页 `GET /users/{id}`
- [x] `P0` **找回密码**（邮件占位 mock/SMTP，`/auth/forgot-password` + `/auth/reset-password`，令牌 sha256 存储/30min/一次性；前端忘记/重置页）
- [ ] `P0` 头像**图片上传**（当前先支持填 URL）→ M6.2（对象存储）
- `P1` 注册邮箱验证；登录限流防爆破；刷新令牌轮换与登出失效
- `P2` 第三方登录（GitHub / 微信）

### 富媒体（对象存储）
- [x] `P0` 存储抽象层（local 本地磁盘默认 + S3 兼容占位，凭证未配自动回退）+ `/uploads` 静态服务
- [x] `P0` **图片上传** `POST /files/images`（类型/大小校验）+ 头像上传（设置页）
- [ ] `P0` 对象存储真实接入（R2/TOS，`S3Storage.save` 补 `put_object`）→ 上线时执行
- [ ] `P1` 内容/投稿配图、聊天图片共用；本地文件/笔记上传（Markdown / PDF）
- [ ] `P0/P1` Markdown 编辑器（代码高亮、粘贴即传图）

### 通知中心
- [x] `P0` 站内通知模型/服务（供各模块调用）+ 列表 / 未读数 / 标记已读 / 全部已读
- [x] `P0` 投稿审核结果通知作者（approved / rejected）；前端通知页 + Navbar 未读角标
- [x] `P0` 评论回复触发通知（M8 互动已接入）；点赞/私信触发待补（M7 私聊）
- `P1` 邮件通知（Resend 等）；通知偏好设置

---

## M7 · 私聊 IM + 社交关系（提前）

参考成熟形态：牛客（面经+讨论+私信+内推）、掘金（关注/点赞/评论）、脉脉（职场社交+私信）、知识星球（付费圈子）、Discord（频道实时）。

### 技术选型（已定基调）
- **自建 WebSocket**：FastAPI WebSocket + Redis pub/sub（多实例广播）+ Postgres 消息表。
- 依赖 Render Starter 常驻（已确认）。规模化后再评估托管 IM（融云/环信/腾讯云 IM）。

### 私信功能
- `P0` 一对一私信：文本、图片、发送/送达/已读、未读数
- `P0` **点头像 → 用户主页 → 发起私聊**
- `P0` 会话列表 + 消息历史（分页）+ 消息通知
- `P0` 反骚扰：拉黑、举报、限流、敏感词、"谁可私信我"设置
- `P1` 在线状态、"正在输入"、消息撤回、离线消息补投
- `P2` 群聊 / 话题频道

### 社交关系
- `P1` 关注 / 粉丝、@提及、个人主页（作品/动态）
- `P1` 动态流（关注的人的更新）

### 架构增量
- WebSocket 网关（鉴权握手复用 JWT）
- 消息存储：`conversations` / `messages` / `message_reads` / `blocks`
- 通知扇出走 M5 的队列；内容安全（敏感词/图片审核可选）

---

## M8 · 互动运营 + 内容完善 + 社区扩展

### 互动
- [x] `P0/P1` 点赞 / 收藏 / 浏览量（通用 interactions 模块，四板块共用；「我的收藏」页）
- [x] `P1` 评论与回复（顶层 + 一级回复，回复触发站内通知；作者/管理员可删）
- [ ] `P1` 评论 @提及、盖楼多级、点赞
- [ ] `P1` 举报 / 审核 / 敏感词（UGC 安全）
- [ ] `P1` 积分商城（积分兑换会员/解锁/实物）

### 内容完善
- [x] `P1` **面经重构 v2**：投稿精简为「标题 / 企业 / 类型(社招·校招·日常实习·暑期实习) / 按轮次(一面·二面·三面·HR面)问答 / 整体感受」；去掉岗位·结果·城市·面试时间·投递渠道
- [x] `P1` **企业面经按类型 4 分类展示**（不再按岗位），每类下以一次完整面试为卡片，左右滑动切换；卡片头展示上传者头像/昵称/涵盖轮次（点头像→公开主页，私聊占位 M7），问答折叠展开，卡片级浏览/点赞/收藏/评论
- [x] `P0` **卡片主人通知**：他人点赞/收藏/评论内容时通知作者（interactions 通用 owner 通知）
- [ ] `P1` 面经结构化问答的**管理后台编辑**（当前后台仅编辑标题 + 类型 + 概述，问答走投稿入口）
- [x] `P1` **八股列表优化**：热度（浏览×1 + 点赞×3 + 收藏×5）默认排序 + 最新排序，分页（每页 20），列表形式 + 收藏/热度/浏览/点赞角标；左侧分类树优化（sticky、层级样式、分组标题）
- [x] `P1` **知识树 MVP**：每个大类一棵可交互知识树（`/knowledge/tree/[categoryId]`），节点旁 + 就地提议子知识点（登录用户走审核；管理员直接上线），叶子可链接八股（点击跳转）；后台「知识树待审」通过/驳回 + 提议者站内通知（模型 `knowledge_nodes`，迁移 0011）
- `P1` 项目 **QA 后台编辑**（当前缺）
- `P1` 标签体系 + 全文搜索（先 Postgres 全文，量大再上 ES）
- `P2` 草稿箱 / 内容版本

### 社区扩展
- `P1` 讨论区 / 帖子（区别于结构化投稿）
- `P2` 圈子 / 话题（可做**付费圈子**，衔接 M5 支付）
- `P2` 等级 / 徽章 / 排行榜

---

## M9 · 合规 / 观测 / 运营后台（持续）

- `P1` 用户管理（封禁、角色、积分调整）；内容管理增强（批量、富文本预览、下架原因）
- `P1` 错误监控（Sentry）+ DB 定时备份 + 告警
- `P0`（若国内公网正式运营）用户协议、隐私政策、**ICP 备案**、内容合规
- `P2` 数据看板（DAU、投稿、收入）、运营位/公告、SEO/OG 图

---

## 成本与风险

- **成本**：Render Starter ~$7/月（常驻）；Neon/Upstash/Vercel 免费档起步，按量增长；微信/支付宝有费率与提现规则。
- **风险**：
  - UGC + 私信带来**内容安全与合规**压力（举报/敏感词/审核不可省）。
  - 私聊长连接的可靠性（离线消息、多端、重连）需要投入；MVP 先保证一对一可用。
  - 免费额度天花板（DB 0.5GB、带宽）到量后需升配。

## 执行方式
- 每个里程碑拆成小 PR（延续现有 vibe coding + PR + CI 流程）。
- 每个 P0 子项都要有测试与端到端验证后再合并。
